<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Log</title>
    {% block styles %}
    <style>
        /* General styles for the table */
        .table-bordered {
            border: 1px solid #dee2e6;
        }

        .table-bordered th, .table-bordered td {
            border: 1px solid #dee2e6;
            padding: 10px;
            text-align: center;
        }

        .table-striped tbody tr:nth-of-type(odd) {
            background-color: #f9f9f9;
        }

        .table th, .table td {
            padding: 12px;
        }

        /* Preformatted text for log */
        pre {
            white-space: pre-wrap; /* Allow wrapping of text */
            word-wrap: break-word; /* Break long words if needed */
            font-size: 1rem; /* Adjust font size */
            line-height: 1.4; /* Improve line height for readability */
            max-width: 100%; /* Allow full width */
            overflow-x: auto; /* Allow horizontal scrolling */
            background-color: #f4f4f4; /* Slight background for contrast */
            padding: 10px;
            border-radius: 5px;
        }

        /* Ensuring responsiveness for small screens */
        @media (max-width: 576px) {
            .table th, .table td {
                font-size: 14px;
                padding: 8px;
            }

            /* Allow horizontal scrolling on small devices for the pre tag */
            pre {
                font-size: 0.9rem; /* Reduce font size slightly */
                padding: 8px;
            }
        }

        /* General scaling for mobile devices */
        body {
            font-size: 16px;
        }
    </style>
    {% endblock %}
</head>
<body>
    <div class="container">
        <!-- Your content here -->
        <pre>{{ log_content }}</pre>
    </div>
</body>
</html>

